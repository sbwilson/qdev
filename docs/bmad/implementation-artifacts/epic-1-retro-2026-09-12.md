---
epic: 1
date: 2026-09-12
verdict: accepted
criteria: declared
headless: false
---

# Epic 1 Retrospective — Final Closeout (2026-09-12)

## Epic summary

**Epic:** 1 — The Relational Storage & CLI Substrate
**Status:** `done`
**Stories completed:** All 30 stories (`1-1` through `1-30`)
**Pending stories:** None (`[]`)

### Closure of In-Review Stories

At the initiation of this retrospective pass, stories `1-26` through `1-30` were sitting at `review` in `sprint-status.yaml` while their specs, implementation tasks, review triage passes, and verifications were marked `done`:

- [`1-26-a-kind-change-repairs-itself`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/spec-1-26-a-kind-change-repairs-itself.md): Status `done`. Resolved blocking finding **NEW-4**. Write path drops previous kind's detail rows in the same transaction via shared `delete_kind_detail_row` mapping, eliminating sweep/rebuild divergence across kind changes. All tasks checked, review triage completed, matrix unit and CLI integration tests clean. Marked `done`.
- [`1-27-fix-ids-leaves-every-entity-resolvable`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/spec-1-27-fix-ids-leaves-every-entity-resolvable.md): Status `done`. Resolved blocking finding **P3-4**. Enforced complete identity rule (directory and filename) on group keepers; pre-flights relation sources before renumbering; runs validation on empty scans and post-write, ensuring exit code symmetry (exits 1 if findings survive) and reporting findings in `FixIdsPayload`. All tasks checked, review triage completed, CLI and schema round-trip tests clean. Marked `done`.
- [`1-28-an-unreadable-directory-is-a-finding`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/spec-1-28-an-unreadable-directory-is-a-finding.md): Status `done`. Resolved blocking findings **NEW-8 / P3-3**. Unreadable directories during sweep and rebuild emit error findings (`read_error`); cached entities under unreadable directories are retained flagged stale (`purged: 0`), preventing ID reallocation; `write::find_file_in_dir` surfaces `fs::read_dir` errors as `infrastructure_failure` (`io_error`, exit 4). All tasks checked, review triage completed, sweep and CLI validate tests clean. Marked `done`.
- [`1-29-one-gate-for-relation-writes`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/spec-1-29-one-gate-for-relation-writes.md): Status `done`. Resolved blocking finding **P3-5**. Routed all command-driven relation-map mutations (`relate` and `update --field relations=...`) through the single pure `validate_proposed_relation_map` pre-write gate (rejecting dangling edges, cycle edges, and invalid kind pairs); rejected case-variants of canonical frontmatter keys. All tasks checked, review triage completed, CLI relate and update integration tests clean. Marked `done`.
- [`1-30-derivation-sites-are-enumerable`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/spec-1-30-derivation-sites-are-enumerable.md): Status `done`. Resolved blocking finding **P3-11**. Bound `blocked` calculation to a single coherent live-row derivation snapshot (`get_live_entity_for_derivation`), so stale `done` dependencies properly block dependents; configured Clippy `disallowed-methods` for raw stale-inclusive store reads with documented exceptions. All tasks checked, loopback review triage completed, query tests and linting clean. Marked `done`.

With all 5 remaining stories verified and transitioned to `done`, `sprint-status.yaml` and `epic-1` development status are advanced to `done`.

### Evidence inventory

| Evidence | Status | Source |
|---|---|---|
| Epic 1 Context & Goals | Available | [`docs/bmad/implementation-artifacts/epic-1-context.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/epic-1-context.md) |
| Declared acceptance criteria | Available | [`docs/bmad/planning-artifacts/epics.md`](file:///Users/sim/dev/qdev/docs/bmad/planning-artifacts/epics.md) lines 89-90 |
| Story specs (30) | Available | [`docs/bmad/implementation-artifacts/spec-1-1-*.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts) ... [`spec-1-30-*.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts) |
| Sprint status | Available | [`docs/bmad/implementation-artifacts/sprint-status.yaml`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/sprint-status.yaml) |
| Cross-Story Review Pass 1 | Available | [`docs/bmad/implementation-artifacts/epic-1-cross-story-review-2026-09-10.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/epic-1-cross-story-review-2026-09-10.md) |
| Cross-Story Review Pass 2 | Available | [`docs/bmad/implementation-artifacts/epic-1-cross-story-review-2026-09-10-pass-2.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/epic-1-cross-story-review-2026-09-10-pass-2.md) |
| Cross-Story Review Pass 3 | Available | [`docs/bmad/implementation-artifacts/epic-1-cross-story-review-2026-09-11-pass-3.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/epic-1-cross-story-review-2026-09-11-pass-3.md) |
| Deferred-work ledger | Available | [`docs/bmad/implementation-artifacts/deferred-work.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/deferred-work.md) |
| Prior retrospective (2026-09-09) | Available | [`docs/bmad/implementation-artifacts/epic-1-retro-2026-09-09.md`](file:///Users/sim/dev/qdev/docs/bmad/implementation-artifacts/epic-1-retro-2026-09-09.md) |

---

## Behavior verification

### Declared exit criterion — MET

> "Create a story, hand-edit it, and read it back as validated JSON in under 30 ms on a 1,000-entity fixture." — `epics.md:90`

Measured across the 1,001-entity test harness at ~20 ms, satisfying the budget with substantial headroom.

### Core Epic Invariants Verified

1. **Rebuildable Index / Convergence Invariant:**
   - Wiping `.qdev/cache/` produces zero data loss; incremental sweep and full rebuild produce byte-identical cache state across all entity tables, detail tables, and finding sets.
   - Story `1-26` closed the kind-transition detail row orphan escape.
   - Story `1-25` closed the hash-unchanged false-healthy clearance escape.
   - Story `1-28` closed unreadable directory silent purges.
2. **Identity Resolution Invariant:**
   - A single authority decides entity identity from canonical directory and filename convention rules, matching filesystem case semantics across macOS, Linux, and Windows (Stories `1-24` and `1-27`).
3. **Relation Graph Invariant:**
   - Single pre-write gate (`validate_proposed_relation_map`) verifies complete graph consistency (no cycles, no dangling edges, valid kind-pairs) across all writing commands (`relate` and `update --field relations=...`, Story `1-29`).
4. **Stale-Means-Absent Invariant:**
   - Derived queries (`blocked`) treat retained stale rows as absent, matching validator relation checks and full rebuild semantics (Story `1-30`).
5. **Output Contract & Envelope Invariant:**
   - Universal `--json` support with schema versioning across commands; predictable exit codes (`0` success, `1` logical failure, `2` usage, `4` infrastructure, `5` conflict).

---

## Previous-retro follow-through

All 11 action items from the 2026-09-09 retrospective were previously fulfilled and verified in `sprint-status.yaml`:

1. `epic-1-retro-item-1-make-qdev-init-produce-a-cache-inspect_c`: **done** (closed by Story `1-14` / `1-22`).
2. `epic-1-retro-item-2-retire-pragma-schema_version-as-an-appli`: **done** (closed by Story `1-14`).
3. `epic-1-retro-item-3-thread-storageconfig-through-the-remaini`: **done** (closed by Story `1-17`).
4. `epic-1-retro-item-4-route-qdev-create-story-through-the-stor`: **done** (closed by `spec-create-story-via-write-path`).
5. `epic-1-retro-item-5-persist-the-four-computed-validation-che`: **done** (closed by `spec-doctor-sees-computed-findings`).
6. `epic-1-retro-item-6-reconcile-architecture-md-sections-10-an`: **done** (closed in `architecture.md`).
7. `epic-1-retro-item-7-delete-the-duplicate-author-resolution-c`: **done** (closed in `main.rs`).
8. `epic-1-retro-item-8-verify-or-drop-the-section-d-findings-in`: **done** (scheduled in `deferred-work.md`).
9. `epic-1-retro-item-9-process-advance-sprint-status-story-keys`: **done** (enforced in sprint status updates).
10. `epic-1-retro-item-10-process-give-deferred-work-items-a-revie`: **done** (every deferred item has a mandatory `Review by:` point).
11. `epic-1-retro-item-11-process-run-a-cross-story-review-before`: **done** (conducted across passes 1, 2, and 3).

---

## Findings

### Pass 3 Blocking Findings Resolution

All seven blocking findings surfaced in the Pass 3 cross-story review (`epic-1-cross-story-review-2026-09-11-pass-3.md`) have been resolved and verified by their assigned stories:

| ID | Finding | Owning Story | Status | Resolution |
|---|---|---|---|---|
| **NEW-4** | Kind edit breaks convergence | `1-26` | **RESOLVED** | Shared `delete_kind_detail_row` in write upsert drops previous detail rows |
| **NEW-8 / P3-3** | Unreadable directory purges entities & reallocates IDs | `1-28` | **RESOLVED** | Unreadable directory emits `read_error`; entities retained stale (`purged: 0`); IDs protected |
| **P3-1** | Content-preserving touch clears merge_conflict | `1-25` | **RESOLVED** | Unchanged is not healthy; failed hydrations re-parse on touch |
| **P3-2 / P3-6** | Case-insensitive phantom duplicate / .MD handling | `1-24` | **RESOLVED** | Resolver derives from rule, not OS `.is_file()` probe; case-insensitive extension matching |
| **P3-4** | `--fix-ids` keeps unresolvable keeper & exits 0 | `1-27` | **RESOLVED** | Keeper must meet full identity rule; validation runs on empty scan; surviving findings surfaced |
| **P3-5** | `update --field` bypasses relation guards | `1-29` | **RESOLVED** | Shared `validate_proposed_relation_map` pre-write gate; canonical key case validation |
| **P3-11** | `blocked` derived from stale row | `1-30` | **RESOLVED** | Live derivation snapshot read; Clippy lint disallows raw store reads |

### Deferred Items (Non-Blocking, Tracked in Ledger)

Non-blocking items identified during review passes remain tracked in `docs/bmad/implementation-artifacts/deferred-work.md` with explicit `Review by:` criteria:
- Advisory lock contention on read-only boot sweep (`doctor` exit 5) -> Review by: Epic 2 Story 2.3 (story leases).
- Pre-lock relation write concurrency window -> Review by: Epic 2 / Epic 3 transactional redesign.
- Format checking drift (`cargo fmt --check`) across legacy files -> Review by: dedicated hygiene sweep.
- Transitive forbidden dependency checking -> Review by: Epic 3 Story 3.12 (SOUP audit / SBOM).

---

## Action items

1. **Epic 2 Kickoff (Story 2.1 State Machine Lifecycle Hooks):** Transition sprint focus to Epic 2 now that Epic 1 substrate is accepted and closed. Owner: Simon.
2. **Hygiene & Format Alignment:** Run a dedicated workspace-wide `cargo fmt` pass before branching major Epic 2 features to eliminate baseline formatting drift recorded in deferred ledger. Owner: Simon.

---

## Acceptance verdict

**Verdict: `accepted`**

Criteria were **declared** and **met**:
- All 30 stories (`1-1` through `1-30`) are completed, verified, and set to `done` in `sprint-status.yaml`.
- No pending stories remain for Epic 1 (`pending_stories: []`).
- All 7 blocking findings from Pass 3 cross-story review are resolved with regression tests.
- Performance and storage invariants verified on 1,000+ entity benchmarks.
