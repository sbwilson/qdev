[package]
name = "qdev-core"
version = "0.1.0"
edition = "2021"
description = "Core domain models, envelope contracts, error taxonomy, and state machine for qdev"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
