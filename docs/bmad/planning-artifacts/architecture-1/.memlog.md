{"type": "init", "topic": "qdev architecture"}
{"type": "decision", "text": "Working mode: Coaching path."}
{"type": "decision", "text": "Deliverable: Developer-focused Architecture Spine only."}
{"type": "decision", "text": "Crate boundary: Strict two-crate workspace (qdev-cli and qdev-core) to decouple CLI parsing from core business logic.", "binds": "Project layout and dependency tree", "prevents": "CLI logic leaking into testable core routines"}
{"type": "decision", "text": "SQLite access: rusqlite (synchronous). No async runtime (tokio) to keep binary lean and simple.", "binds": "Database access layer", "prevents": "Introduction of heavy async runtimes for CLI commands"}
{"type": "decision", "text": "Concurrency strategy: SQLite WAL mode with busy_timeout. Transactions must be short and atomic.", "binds": "Database connection initialization and transaction lifecycle", "prevents": "Database locking and corruption from multiple concurrent CLI invocations"}
{"type": "decision", "text": "Persistence Strategy: File-Backed Cache. Source of truth is structured flat files (JSON/YAML) per entity. SQLite acts as a fast relational index over these files.", "binds": "Storage architecture and source of truth", "prevents": "Merge conflict hell from monolithic ledger dumps and slow commit hooks, while keeping files human-readable"}
{"type": "decision", "text": "Write-Through Mutator: Direct filesystem edits to qdev state files (Epics/Stories) by LLMs are forbidden. They must pipe through the qdev CLI via patch files or block searching. Standard source code is exempt.", "binds": "CLI write interfaces and file locking", "prevents": "Race conditions and corrupted state files from concurrent LLM agents"}
{"type": "decision", "text": "Gate Extensibility: Sub-process scripts. Gates are user-defined executables/scripts in `.qdev/gates/`.", "binds": "Gate engine execution model", "prevents": "Hardcoding project-specific compliance checks into the qdev binary"}
{"type": "decision", "text": "Hydration Algorithm: Directory mtime sweep. Rust sweeps file mtimes on boot to incrementally hydrate SQLite. No daemons or lazy-loading permitted.", "binds": "Core boot sequence", "prevents": "Slow boots and out-of-sync local reads"}
{"type": "event", "text": "spine finalized"}
