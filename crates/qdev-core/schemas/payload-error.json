{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Error Payload Schema",
  "description": "Output shape of the AD-13 JSON error envelope, emitted on stdout in JSON mode by any qdev command that fails. Schema changes follow semver on schema_version; additive fields do not bump the major.",
  "type": "object",
  "required": ["schema_version", "error"],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "error": {
      "type": "object",
      "required": ["code", "message"],
      "properties": {
        "code": {
          "type": "string",
          "minLength": 1
        },
        "message": {
          "type": "string"
        },
        "details": {
          "description": "Optional structured context; shape varies by error code."
        }
      }
    }
  }
}
